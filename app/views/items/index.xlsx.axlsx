wb = xlsx_package.workbook
wb.styles do |style|
  highlight_cell = style.add_style(bg_color: "EFC376")

  wb.add_worksheet(name: "ItemSub") do |sheet|
    @items.each do |product|
     sheet.add_row [product.item_number], style: [nil, highlight_cell]
    end
  end
end